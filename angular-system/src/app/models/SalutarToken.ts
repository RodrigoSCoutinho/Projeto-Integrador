export class SalutarToken {
    token: string = '';
    // usuario: string = '';
    // expiracao: number = 0;

    // constructor(token: string, usuario: string, expiracao: number) {
    //     this.token = token;
    //     this.usuario = usuario;
    //     this.expiracao = expiracao;
    // }
}